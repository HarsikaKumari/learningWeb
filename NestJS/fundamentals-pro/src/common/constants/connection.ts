export const connection: Connection = {
    CONNECTION_STRING: "hello how are you",
    DB: "postgreSQL",
    DBNAME: "learningNest",
};

export type Connection = {
    CONNECTION_STRING: string,
    DB: string,
    DBNAME: string,
};
